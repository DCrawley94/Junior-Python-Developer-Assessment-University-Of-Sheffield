# Leave DB_HOST blank to connect via Unix socket (peer auth, no password needed).
# Set to localhost to connect via TCP (password required).
DB_HOST=
DB_PORT=5432
DB_NAME=sheffield_assessment
DB_USER=your_user
DB_PASSWORD=
TEST_DB_NAME=sheffield_assessment_test
